[gd_scene load_steps=6 format=2]

[ext_resource path="res://levels/traps/rocketlauncher/rocketlauncher.png" type="Texture" id=1]
[ext_resource path="res://bosses/darko/attacks/first/bloodball.png" type="Texture" id=2]
[ext_resource path="res://bosses/darko/attacks/second/lasergun.gd" type="Script" id=3]

[sub_resource type="Shader" id=1]
code = "shader_type particles;
float rand_from_seed(inout uint seed) {
	int k;
	int s = int(seed);
	if (s==0) {
		s = 305420679;
	}
	k = s / 127773;
	s = 16807 * (s - k * 127773) - 2836 * k;
	if (s < 0) {
		s += 2147483647;
	}
	seed = uint(s);
	return float(seed % uint(65536)) / 65535.0;
}
float rand(float co) { return fract(sin(co*(91.3458)) * 47453.5453); }
void vertex() {
	float speed = 200.0;
	uint seed = NUMBER + RANDOM_SEED;
	if(RESTART) {
		COLOR.a = 0.2;
		TRANSFORM[3].xyz = vec3((rand(rand_from_seed(seed))-0.5)*200.0,(rand(rand_from_seed(seed))-0.5)*200.0,0.0);
	}
	vec3 move_vector = vec3(-TRANSFORM[3].x,-TRANSFORM[3].y,0.0);
	vec3 direction = normalize(move_vector);
	VELOCITY = direction * speed * (length(move_vector))/20.0;
	COLOR.a += DELTA*2.0;
	
}"

[sub_resource type="ShaderMaterial" id=2]
shader = SubResource( 1 )

[node name="Lasergun" type="Node2D"]
script = ExtResource( 3 )

[node name="Sprite" type="Sprite" parent="."]
texture = ExtResource( 1 )

[node name="shootpos" type="Position2D" parent="."]
position = Vector2( 13, 0 )

[node name="Particles2D" type="Particles2D" parent="."]
modulate = Color( 1.7, 1.3, 1.3, 1 )
emitting = false
amount = 34
lifetime = 0.3
one_shot = true
preprocess = 0.1
randomness = 0.82
process_material = SubResource( 2 )
texture = ExtResource( 2 )
